PORT=4423
DATABASE_PATH=./data/hair-expo.sqlite
PAYMENT_PROVIDER=fake
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me
FRONTEND_URL=http://localhost:4421
# Comma-separated exact browser origins; defaults to FRONTEND_URL.
CORS_ALLOWED_ORIGINS=http://localhost:4421
APP_PASSCODE=change-me
AUTH_MAX_ATTEMPTS=5
AUTH_RATE_LIMIT_WINDOW_SECONDS=900
CATALOG_CSV_PATH=./data/trunov_price_list.csv
CHECKOUT_MAX_QUANTITY=10000

# STRIPE_PUBLISHABLE_KEY is currently unused because checkout is created server-side.
# Keep it out of the backend runtime unless a future frontend Stripe.js integration needs it.
